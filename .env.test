# Test environment configuration for ETL Pipeline
# These credentials are used for testing purposes only

# Database configuration (matches docker-compose.yml defaults)
DB_NAME=etl_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=mysecretpassword

# AWS credentials (mocked in tests using moto)
aws_access_key_id=testing
aws_secret_access_key_id=testing

# Test-specific settings
TEST_S3_BUCKET=test-bucket
TEST_S3_REGION=us-east-1
